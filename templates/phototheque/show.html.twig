{% extends 'base.html.twig' %}

{% block title %}Phototheque{% endblock %}

{% block body %}
    <h1>Phototheque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phototheque.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ phototheque.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ phototheque.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phototheque_index') }}">back to list</a>

    <a href="{{ path('phototheque_edit', {'id': phototheque.id}) }}">edit</a>

    {{ include('phototheque/_delete_form.html.twig') }}
{% endblock %}
