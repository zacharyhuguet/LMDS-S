{% extends 'base.html.twig' %}

{% block body %}
	<br/><br/><br/>
	<div class="container-produit-header">
		<h1>Nos téléphones
		</h1>
		<h3>Tout nos téléphones sont reconditionnées directement dans notre magasin, et sont proposer à un prix abordable pour notre clientèle !<h3></div>*
			<hr class="separateur-hr">
			<div class="container-produit">
				{% for produit in produits %}
					<div class="product">
						<img alt="&nbsp;Image indisponible" src="/imagesProduits/{{produit.image }}"/>
						<label class="price">
							{% if produit.ancienPrix is not empty %}
								<em>{{ produit.ancienPrix }}
									€
								</em>
								&nbsp;&nbsp;&nbsp;
							{% endif %}

							{{ produit.prix }}
							€
						</label>

						<div class="desciption">
							<h2 class="title">{{ produit.titre }}</h2>
							<a class="lien-show" href="{{ path('produits_show', {'id': produit.id}) }}">
								<i class="fas fa-plus"></i>
								Voir plus</a>
						</div>
					</div>

				{% endfor %}
			</div>


		{% endblock %}
