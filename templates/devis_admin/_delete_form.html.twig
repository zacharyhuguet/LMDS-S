<form method="post" action="{{ path('devis_admin_delete', {'id': devi.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce devis (n°{{devi.id}}) ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devi.id) }}">
    <button class='btn btn-danger'><i class='fas fa-trash-alt'></i></button>
</form>