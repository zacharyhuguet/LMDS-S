<form method="post" action="{{ path('information_accueil_delete', {'id': information_accueil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ information_accueil.id) }}">
    <button class="btn">Delete</button>
</form>
