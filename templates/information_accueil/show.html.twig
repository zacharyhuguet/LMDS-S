{% extends 'magasin.html.twig' %}


{% block body %}
    <h1>InformationAccueil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ information_accueil.id }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ information_accueil.emplacement }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ information_accueil.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ information_accueil.texte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('information_accueil_index') }}">back to list</a>

    <a href="{{ path('information_accueil_edit', {'id': information_accueil.id}) }}">edit</a>

    {{ include('information_accueil/_delete_form.html.twig') }}
{% endblock %}
